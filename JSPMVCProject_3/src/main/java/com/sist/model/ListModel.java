package com.sist.model;
import java.util.*;
import com.sist.dao.*;

public class ListModel implements model {
	
	public String exe
	
	{
		String page=request.getparameter("page");
		if(page==null) {
			page=
		}
	}
}
